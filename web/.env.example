# Used by `node build.mjs` at deploy time (Vercel project env vars).
APPLE_TEAM_ID=
ANDROID_SHA256=
APP_STORE_ID=
# Optional overrides:
# IOS_BUNDLE_ID=com.noticeboard.app
# ANDROID_PACKAGE=com.noticeboard.app
